10 DEF FNA(X)=1+FNA(X)
20 A$="123456"+"a"
30 B$="67890"+"1"
40 A = VARPTR(A$)
45 OPEN "output" FOR OUTPUT AS 1
50 PRINT#1, PEEK(A), PEEK(A+1), PEEK(A+2)
60 A$="defght"+"d"
70 A = VARPTR(A$)
80 PRINT#1, PEEK(A), PEEK(A+1), PEEK(A+2)
90 ON ERROR GOTO 1000
100 PRINT#1, FNA(1)
110 A = VARPTR(A$)
120 PRINT#1, PEEK(A), PEEK(A+1), PEEK(A+2)
130 PRINT#1, FRE("")
140 A = VARPTR(A$)
150 PRINT#1, PEEK(A), PEEK(A+1), PEEK(A+2)
160 END
1000 PRINT#1, ERR, ERL
1010 RESUME NEXT

